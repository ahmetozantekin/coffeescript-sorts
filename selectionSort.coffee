class Sort
    selectionSort:->
        
        arr = [4, 7, 8, 3]
        len = arr.length
        i = 0
        
        while i < len
            min = i
            j = i + 1
            
            while j < len
                if arr[j] < arrmin]
                    min = j
            j++
   
            
    unless i = min
      swap arr, i, min
    i++
    
  arr
